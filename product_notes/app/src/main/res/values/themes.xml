<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Product_notes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/default_color</item>
        <item name="colorPrimaryVariant">@color/default_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!--        <item name="windowNoTitle">true</item>-->

        <!-- Hide Action bar. -->
        <item name="windowActionBar">false</item>

    </style>

    <style name="ThemeOverlay.Product_notes.AppWidgetContainer" parent="">
        <item name="appWidgetBackgroundColor">#FFECDC</item>
        <item name="appWidgetTextColor">@color/black</item>
    </style>

    <style name="AppThemeNote">
        <item name="colorOnPrimary">@color/white</item>

    </style>

    <style name="splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">

    </style>
</resources>